export class  comentario {
  userName: string;
  comentario: string;

  constructor(
    userName: string,
    comentario: string,
    ){
      this.userName = userName;
      this.comentario = comentario;
  }
}
